import { FC } from 'react'

export const NotFoundPage: FC = () => {
  return (
    <div className='text-muted-foreground mx-auto flex min-h-screen max-w-6xl items-center justify-center'>
      요청한 페이지를 찾을 수 없습니다 🚫
    </div>
  )
}
